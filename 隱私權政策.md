—————— 隱私權政策 — Travel in Time 生效日期：2026-07-07

簡介
歡迎使用 Travel in Time（以下稱「本應用」或「本服務」）。我們重視使用者隱私與資料安全，特此說明本應用如何收集、使用、儲存及刪除資料。本政策適用於你使用本應用（包含行動 App、PWA 與桌面版本）。

我們蒐集哪些資料

使用者提供（必要、由你上傳或建立）
電子書檔案（epub 等）、書籍檔名、書籍封面（若使用者上傳或由應用擷取）。
使用者建立的內容：註記（highlight/notes）、書籤、閱讀進度、收藏、書庫中的資料等。
使用者在應用內輸入的文字（例如註解文字、書名修改等）。
裝置與應用狀態（僅本機保存）
本機設定與偏好（字體、字型大小、主題、TTS 設定等）。
程式版本資訊（用於檢查更新時向更新伺服器發送的基本版本請求）。
第三方連線（非個人資料，但會與第三方伺服器通訊）
Google Fonts（fonts.googleapis.com / fonts.gstatic.com）：用以載入遠端字型時會向 Google 發出請求。
自動更新：Electron 版使用 auto-update 機制檢查更新，檢查更新時會向設定的更新服務（例如 GitHub Releases 或其他更新主機）發送請求。
重要說明：目前（依本倉庫程式碼）本應用並未整合任何分析/追蹤套件（例如 Google Analytics / Firebase Analytics）、廣告 SDK（例如 AdMob）、地理位置 API、相機或麥克風授權，也未在程式碼中看到會自動把使用者書籍或註記上傳到外部伺服器的實作。README/開發計畫中提到「未來可能串接 OpenAI TTS / AI 摘要」，但那尚未在程式碼中實作——若你在未來加入雲端 AI 服務或後端備份，需在隱私權政策與 Play Console 中更新揭露。

資料用途
提供基本功能：在本機儲存電子書、展示封面、顯示與管理註記、書籤及閱讀進度等。
檢查更新：Electron 的自動更新機制會檢查是否有新版，僅為版本更新用途。
運行字型載入：載入 Google Fonts 以改善字型顯示（若使用遠端字型）。
資料儲存與保存地點
多數資料僅儲存在使用者裝置上（例如：IndexedDB、localStorage、SQLite、App 沙盒檔案系統），不會自動上傳至我們的伺服器（除非你特別啟用或未來新增雲端備份功能）。
第三方請求（如 Google Fonts、更新伺服器）會在載入或檢查更新時發出 HTTPS 請求。這些請求可能會使第三方接收到裝置的 IP 位址與相關連線資訊。
資料分享與第三方
目前我們不會將使用者的書籍或註記分享或出售給第三方。若未來需與第三方（例如 AI 服務供應商）分享資料，會在功能啟用前在應用內與隱私權政策中明確告知並取得使用者同意。
第三方清單（依現況）
Google Fonts（fonts.googleapis.com / fonts.gstatic.com）：載入網路字型（可能送出請求到 Google）。
更新伺服器（electron-updater 所連線之主機，視你發佈所用的更新來源而定，常見為 GitHub Releases 或自建更新主機）。
如有整合雲端 AI 或同步/備份服務，請務必在此處列出供應商名稱與其隱私政策連結。
Cookie 與類似技術（PWA / 網頁版）
本 PWA 使用瀏覽器儲存（IndexedDB、Cache Storage、localStorage）來保存使用者書籍與設定；不會設置第三方追蹤 cookie。
若你透過瀏覽器使用 PWA，瀏覽器與第三方資源（例如 Google Fonts）仍會進行標準的 HTTP 請求。
資料安全
我們採取合理的技術與組織措施以保護本機儲存資料的安全（例如限制檔案讀取權限、使用安全的通訊協議 HTTPS 進行外部請求）。
由於應用目前以本地為主，建議使用者妥善保管裝置與備份；若使用者在裝置遺失或被破解，本地資料亦有被他人存取的風險。
資料保留
在本地儲存的資料會一直保留，除非使用者主動刪除或清除應用資料。若你日後加入雲端備份功能，會在功能說明中註明保留期限與刪除機制。
如何刪除資料（data deletion 指引） 我們提供數種刪除資料的方式，請依你使用的版本選擇對應步驟：
A. 行動 App（Android / iOS）內刪除單一書籍或註記

刪除單本書或書籍資料：打開應用 → 前往「書庫」→ 長按或選擇該書 → 點「刪除」或垃圾桶圖示，確認刪除以移除該書及其本機註記/進度（實際 UI 名稱請依應用版本）。
刪除註記/書籤：打開該書 → 前往筆記/書籤面板 → 選擇欲刪除項目 → 刪除。
B. 刪除整個應用資料（恢復到初始狀態）

Android：設定 > 應用程式 > Travel in Time > 儲存空間 > 清除資料（或清除快取）即可刪除應用本機資料與設定。
iOS：刪除 App 並重新安裝，或在「iPhone 儲存空間」內刪除 App 資料。
C. PWA（在瀏覽器中）

在瀏覽器內清除網站資料（Cookies、IndexedDB、Cache Storage）：各瀏覽器操作不同，通常在「設定 > 隱私與安全性 > 清除瀏覽資料」或網站權限管理中移除 Travel in Time 的站點資料。
Chrome 範例：設定 > 隱私權與安全性 > 網站設定 > 查看所有網站權限 > 搜尋你的網站（或 localhost）並清除資料。
D. 桌面版（Electron）

若使用 Electron 桌面版，請在系統中刪除應用資料資料夾（通常位於使用者資料目錄中的 app data）；或在應用內提供的刪除/移除功能執行刪除。具體路徑依作業系統而異（Windows: %APPDATA%/YourApp，macOS: ~/Library/Application Support/YourApp）。
E. 要求我們協助刪除（若你啟用了任何雲端功能）

若你啟用了任何會上傳到我們伺服器的功能，或需要我們代為刪除伺服器備份／帳戶資料，請以電子郵件聯絡我們：support@retsomm.dev（請替換為你的正式聯絡信箱）。我們會在收到請求後用 30 天內回應並處理刪除事宜。請在郵件內提供必要識別資訊（例如裝置 ID、書籍 ID 或其他可協助識別的資訊）。
兒童隱私
本應用並非針對 13 歲以下兒童設計（或依各國法規調整最低年齡）。若你為家長並發現兒童上傳個人資訊，請聯絡我們以便進行資料刪除。
隱私權政策變更
本政策可能會隨著功能更新與法規變動而修訂；任何重大變更會在應用內公告或更新本頁面的生效日期。建議定期查看此政策。
聯絡方式
若對本隱私權政策有疑問或想行使資料刪除權、查詢權，請聯絡：support@retsomm.dev（請替換為你的正式聯絡信箱），或於 GitHub 倉庫提出 issue（https://github.com/Retsomm/TravelInTime）。
Google Play Data Safety（建議如何在 Play Console 填寫）
資料類別（建議勾選/不勾選）：
資料傳送給第三方：否（除非你未來加入雲端/同步/AI 功能，或第三方 SDK）。
個人資訊 (Identifiers)：否（若你不蒐集使用者登入帳號或 email）。
使用者內容（Files & Docs / User‑provided content）：是（因為使用者會上傳電子書與建立註記）。
裝置/應用記錄與診斷：否（目前無 analytics）。
位置：否（程式碼中沒有使用地理位置 API）。
廣告識別碼 / 廣告：否（未整合廣告 SDK）。
共享：否（目前不會分享使用者內容給第三方）。
若未來加入雲端或 AI：
若系統會上傳書籍或片段至 AI 服務（例如 OpenAI）進行 TTS/摘要，請在 Play Console 勾選「會將使用者內容傳送至第三方／用於分析或個人化」並在隱私政策中追加對應說明與供應商資訊。
其他事項
本政策不構成法律意見。如需合規保證（例如 GDPR、CCPA、Google Play 的特殊要求），建議諮詢專業的法務或隱私顧問，並根據目標市場調整政策內容。